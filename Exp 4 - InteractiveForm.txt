Experiment No: 4
Title: Interactive Form using Flutter

Aim:
To design an interactive form using various Flutter form widgets. 

Requirements:

* FlutLab account
* Internet

Steps:

1. Open https://flutlab.io

2. Click "New Project"

3. Enter project name: form_app

4. Click "Create Project"

5. Open lib/main.dart

6. Delete existing code

7. Paste the following code

import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    debugShowCheckedModeBanner: false,
    theme: ThemeData(
      useMaterial3: true,
      colorSchemeSeed: Colors.deepPurple,
    ),
    home: LoginPage(),
  ));
}

class LoginPage extends StatelessWidget {
  final _nameController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: double.infinity,
        decoration: BoxDecoration(
          gradient: LinearGradient(
            colors: [Colors.deepPurple.shade900, Colors.black],
            begin: Alignment.topCenter,
            end: Alignment.bottomCenter,
          ),
        ),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Icon(Icons.rocket_launch, size: 80, color: Colors.white),
            const SizedBox(height: 20),
            const Text(
              "FocusFlow",
              style: TextStyle(
                color: Colors.white,
                fontSize: 32,
                fontWeight: FontWeight.bold,
              ),
            ),
            const SizedBox(height: 40),

            Padding(
              padding: const EdgeInsets.symmetric(horizontal: 30),
              child: Card(
                color: Colors.white.withOpacity(0.1),
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(20),
                ),
                child: Padding(
                  padding: const EdgeInsets.all(20),
                  child: Column(
                    children: [
                      TextField(
                        controller: _nameController,
                        style: const TextStyle(color: Colors.white),
                        decoration: const InputDecoration(
                          hintText: "Enter Name",
                          hintStyle: TextStyle(color: Colors.white70),
                          prefixIcon:
                              Icon(Icons.person, color: Colors.white70),
                          border: InputBorder.none,
                        ),
                      ),
                      const Divider(color: Colors.white24),
                      const TextField(
                        obscureText: true,
                        style: TextStyle(color: Colors.white),
                        decoration: InputDecoration(
                          hintText: "Password",
                          hintStyle: TextStyle(color: Colors.white70),
                          prefixIcon:
                              Icon(Icons.lock, color: Colors.white70),
                          border: InputBorder.none,
                        ),
                      ),
                    ],
                  ),
                ),
              ),
            ),

            const SizedBox(height: 30),

            ElevatedButton(
              style: ElevatedButton.styleFrom(
                backgroundColor: Colors.deepPurpleAccent,
                padding:
                    const EdgeInsets.symmetric(horizontal: 100, vertical: 15),
              ),
              onPressed: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(
                    builder: (context) =>
                        MainScreen(userName: _nameController.text),
                  ),
                );
              },
              child: const Text("GET STARTED"),
            ),
          ],
        ),
      ),
    );
  }
}

class MainScreen extends StatefulWidget {
  final String userName;

  MainScreen({required this.userName});

  @override
  State<MainScreen> createState() => _MainScreenState();
}

class _MainScreenState extends State<MainScreen> {
  int _currentIndex = 0;

  List<String> tasks = ["Learn Flutter", "Go to Gym"];

  void addTask(String task) {
    setState(() {
      tasks.add(task);
    });
  }

  void deleteTask(int index) {
    setState(() {
      tasks.removeAt(index);
    });
  }

  @override
  Widget build(BuildContext context) {
    final pages = [
      DashboardPage(name: widget.userName, count: tasks.length),
      TaskPage(tasks: tasks, onAdd: addTask, onDelete: deleteTask),
      ProfilePage(name: widget.userName),
    ];

    return Scaffold(
      body: pages[_currentIndex],
      bottomNavigationBar: NavigationBar(
        selectedIndex: _currentIndex,
        onDestinationSelected: (index) {
          setState(() {
            _currentIndex = index;
          });
        },
        destinations: const [
          NavigationDestination(icon: Icon(Icons.home), label: "Home"),
          NavigationDestination(icon: Icon(Icons.list), label: "Tasks"),
          NavigationDestination(icon: Icon(Icons.person), label: "Profile"),
        ],
      ),
    );
  }
}

class DashboardPage extends StatelessWidget {
  final String name;
  final int count;

  DashboardPage({required this.name, required this.count});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hi ${name.isEmpty ? "User" : name}")),
      body: Center(
        child: Text("Total Tasks: $count"),
      ),
    );
  }
}

class TaskPage extends StatelessWidget {
  final List<String> tasks;
  final Function(String) onAdd;
  final Function(int) onDelete;

  TaskPage(
      {required this.tasks, required this.onAdd, required this.onDelete});

  final TextEditingController controller = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Tasks")),
      body: ListView.builder(
        itemCount: tasks.length,
        itemBuilder: (context, index) {
          return ListTile(
            title: Text(tasks[index]),
            trailing: IconButton(
              icon: const Icon(Icons.delete),
              onPressed: () => onDelete(index),
            ),
          );
        },
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {
          showDialog(
            context: context,
            builder: (context) => AlertDialog(
              title: const Text("Add Task"),
              content: TextField(controller: controller),
              actions: [
                TextButton(
                  onPressed: () => Navigator.pop(context),
                  child: const Text("Cancel"),
                ),
                ElevatedButton(
                  onPressed: () {
                    if (controller.text.isNotEmpty) {
                      onAdd(controller.text);
                      controller.clear();
                      Navigator.pop(context);
                    }
                  },
                  child: const Text("Add"),
                ),
              ],
            ),
          );
        },
        child: const Icon(Icons.add),
      ),
    );
  }
}

class ProfilePage extends StatelessWidget {
  final String name;

  ProfilePage({required this.name});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        name.isEmpty ? "Guest User" : name,
        style: const TextStyle(fontSize: 24),
      ),
    );
  }
}

8. Click "Run"

Output:
An interactive app with login form, dashboard, task manager and profile screen will be displayed. 

Conclusion:
In this experiment, an interactive form was created using various Flutter widgets. 




Description:

In this experiment, an interactive multi-screen application was designed and developed using Flutter form widgets on the FlutLab platform. The primary objective was to understand how user input can be captured, processed, and utilized across different screens in a Flutter application.

The application begins with a login interface that incorporates essential form elements such as TextField, TextEditingController, and styled input components to capture user credentials. The UI design leverages gradient backgrounds, cards, and icons to create a modern and engaging user experience.

Upon submission, the application navigates to a main screen using Flutter’s navigation system, demonstrating the use of Navigator and MaterialPageRoute. The main interface is structured using a bottom navigation bar, allowing users to switch between multiple screens—Dashboard, Task Manager, and Profile.

State management is implemented using StatefulWidget, enabling dynamic updates such as adding and deleting tasks in real time. The ListView.builder widget is used to efficiently render a list of tasks, while dialog boxes (AlertDialog) provide an intuitive way to input new data.

Overall, this experiment highlights the integration of form handling, navigation, and state management concepts to build a functional and interactive Flutter application.



Conclusion (Improved):

In this experiment, an interactive Flutter application was successfully developed using various form and layout widgets. The application includes a login form, navigation between multiple screens, and a task management system with real-time updates.

The key activities performed involved capturing user input, implementing screen navigation, managing application state, and dynamically updating UI components. This experiment emphasizes the importance of interactivity in modern applications and demonstrates how Flutter facilitates seamless user experience through its rich set of widgets.

The most important learning outcome is understanding how form inputs, state management, and navigation work together to create a complete and functional application. This experiment provides a strong foundation for developing advanced Flutter applications with user interaction and dynamic data handling.