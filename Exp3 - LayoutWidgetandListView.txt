Experiment No: 3
Title: ListView UI using Flutter Layout Widgets

Aim:
To design a layout of Flutter app using layout widgets and create a ListView UI. 

Requirements:

* FlutLab account
* Internet

Steps:

1. Open https://flutlab.io

2. Click on "New Project"

3. Enter project name: employee_list_app

4. Click "Create Project"

5. Go to lib/main.dart

6. Delete existing code

7. Copy and paste the following code:

```dart
import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
    debugShowCheckedModeBanner: false,
    home: EmployeeListScreen(),
  ));
}

class EmployeeListScreen extends StatelessWidget {
  const EmployeeListScreen({super.key});

  final List<Map<String, String>> employees = const [
    {
      "name": "Rahul Sharma",
      "role": "Flutter Developer",
      "img": "https://i.pravatar.cc/150?u=1",
      "status": "Online"
    },
    {
      "name": "Sneha Patil",
      "role": "UI/UX Designer",
      "img": "https://i.pravatar.cc/150?u=2",
      "status": "Away"
    },
    {
      "name": "Amit Verma",
      "role": "Backend Engineer",
      "img": "https://i.pravatar.cc/150?u=3",
      "status": "Online"
    },
    {
      "name": "Priya Das",
      "role": "Product Manager",
      "img": "https://i.pravatar.cc/150?u=4",
      "status": "Offline"
    },
    {
      "name": "Vikram Singh",
      "role": "QA Analyst",
      "img": "https://i.pravatar.cc/150?u=5",
      "status": "Online"
    },
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        decoration: const BoxDecoration(
          gradient: LinearGradient(
            begin: Alignment.topCenter,
            end: Alignment.bottomCenter,
            colors: [Color(0xFF0D47A1), Color(0xFF1976D2)],
          ),
        ),
        child: Column(
          children: [
            const SizedBox(height: 60),

            const Text(
              "Our Elite Team",
              style: TextStyle(
                color: Colors.white,
                fontSize: 28,
                fontWeight: FontWeight.bold,
              ),
            ),

            const SizedBox(height: 10),

            Text(
              "${employees.length} Members Active",
              style: TextStyle(
                color: Colors.white.withOpacity(0.8),
                fontSize: 16,
              ),
            ),

            const SizedBox(height: 20),

            Expanded(
              child: Container(
                padding: const EdgeInsets.only(top: 20),
                decoration: const BoxDecoration(
                  color: Colors.white,
                  borderRadius: BorderRadius.only(
                    topLeft: Radius.circular(30),
                    topRight: Radius.circular(30),
                  ),
                ),
                child: ListView.builder(
                  padding: const EdgeInsets.symmetric(horizontal: 20),
                  itemCount: employees.length,
                  itemBuilder: (context, index) {
                    final emp = employees[index];

                    return Container(
                      margin: const EdgeInsets.only(bottom: 15),
                      padding: const EdgeInsets.all(12),
                      decoration: BoxDecoration(
                        color: Colors.white,
                        borderRadius: BorderRadius.circular(15),
                        boxShadow: [
                          BoxShadow(
                            color: Colors.black.withOpacity(0.05),
                            blurRadius: 10,
                            offset: const Offset(0, 4),
                          ),
                        ],
                      ),
                      child: Row(
                        children: [
                          Stack(
                            children: [
                              CircleAvatar(
                                radius: 35,
                                backgroundImage:
                                    NetworkImage(emp['img']!),
                              ),
                              Positioned(
                                bottom: 2,
                                right: 2,
                                child: Container(
                                  height: 15,
                                  width: 15,
                                  decoration: BoxDecoration(
                                    color: emp['status'] == "Online"
                                        ? Colors.green
                                        : Colors.orange,
                                    shape: BoxShape.circle,
                                    border: Border.all(
                                      color: Colors.white,
                                      width: 2,
                                    ),
                                  ),
                                ),
                              ),
                            ],
                          ),

                          const SizedBox(width: 15),

                          Expanded(
                            child: Column(
                              crossAxisAlignment: CrossAxisAlignment.start,
                              children: [
                                Text(
                                  emp['name']!,
                                  style: const TextStyle(
                                    fontWeight: FontWeight.bold,
                                    fontSize: 18,
                                  ),
                                ),
                                const SizedBox(height: 4),
                                Text(
                                  emp['role']!,
                                  style: const TextStyle(
                                    color: Colors.grey,
                                  ),
                                ),
                              ],
                            ),
                          ),

                          Container(
                            padding: const EdgeInsets.all(8),
                            decoration: BoxDecoration(
                              color: Colors.blue[50],
                              borderRadius: BorderRadius.circular(10),
                            ),
                            child: const Icon(
                              Icons.chat_bubble_outline,
                              color: Colors.blue,
                            ),
                          ),
                        ],
                      ),
                    );
                  },
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

8. Click on "Run"

Output:
A ListView displaying employee cards with name, role, image, and status indicator will be shown. 

Conclusion:
In this experiment, a layout was designed using various Flutter layout widgets. 



Description:

In this experiment, a dynamic and visually appealing employee list interface was designed using Flutter layout widgets on the FlutLab platform. The primary objective was to understand how to effectively use layout and scrolling widgets to display structured data in a mobile application.

The implementation makes use of essential Flutter widgets such as Scaffold, Container, Column, Row, Expanded, and Stack to organize and align UI components efficiently. A gradient background was applied to enhance the visual aesthetics, while a rounded container layout was used to create a modern card-based interface.

The core component of this experiment is the ListView.builder, which enables efficient rendering of a scrollable list of employee data. Each list item is dynamically generated using a data collection (list of maps), demonstrating how Flutter handles repeated UI elements efficiently. Additional styling elements such as BoxDecoration, BoxShadow, and BorderRadius were incorporated to provide depth and improve the overall user experience.

Furthermore, the use of CircleAvatar for profile images and Stack for overlaying status indicators (online/away/offline) showcases how multiple widgets can be combined to create complex UI elements. This experiment emphasizes responsiveness, reusability, and clean UI structuring techniques.


Conclusion (Improved):

In this experiment, a fully functional ListView-based UI was successfully developed using Flutter layout widgets. The application displays employee information in a structured and scrollable format, including name, role, profile image, and status indicator.

The key tasks performed included designing the layout using nested widgets, implementing a dynamic list using ListView.builder, and enhancing the UI with styling and alignment techniques. This experiment highlights the importance of efficient data representation and performance optimization when dealing with large or dynamic datasets.

The most important learning outcome is understanding how layout widgets and list-building techniques work together to create scalable and responsive user interfaces. Overall, this experiment strengthens practical knowledge of Flutter UI design and prepares for building real-world applications involving dynamic content.